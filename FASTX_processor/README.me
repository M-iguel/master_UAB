Implement a python tool that, given a FASTA or FASTQ file, preprocess all the sequences contained in it. The tool has to be able to perform: reverse-complement, trim, and adaptor removal functions depending on the command line options.
The tool has to be able to work with both FASTA/FASTQ file formats (i.e., it must resolve the input format of the file provided by the
user). In this way, the output of the execution has to be in the sameformat as the input
